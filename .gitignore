# Source regulation PDFs (large; not needed at runtime — rules are in data/rules/*.json)
*.pdf
# Working text extracts & samples (derived, not needed at runtime)
data/text/
data/samples/
# Local database (created at runtime / persisted on the host disk)
data/projects.db
*.db
# Python
__pycache__/
*.pyc
.venv/
venv/
# Editor / OS
.DS_Store
Thumbs.db
# Local-only Claude tooling config
.claude/

# --- Reference books (Neufert Data feature): ship these despite the *.pdf / *.db rules ---
# Team-only deployment needs the PDFs (page rendering), the FTS db, and the topic index.
!data/books/neufert.pdf
!data/books/architects_data.pdf
!data/books/books.db
!data/books/books_index.json
# but NOT the regenerable rendered-page image cache
data/books/cache/
